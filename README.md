# InsightCore - Платформа видеоаналитики

InsightCore - это платформа видеоаналитики с открытым исходным кодом, предназначенная для объектов нефти и газа (автозаправочные станции, нефтяные терминалы), которая обеспечивает мониторинг в реальном времени, обнаружение объектов и автоматические оповещения с использованием компьютерного зрения и ИИ. Платформа работает на CPU (Intel) без необходимости ускорения с помощью GPU.

## 🚀 Возможности

### Основная функциональность
- **Анализ видео в реальном времени**: Обнаружение, отслеживание и анализ поведения объектов
- **Поддержка нескольких камер**: Обработка до 100 камер на одном сервере Intel
- **Аналитика на основе правил**: Настраиваемые правила для пересечения линии, нарушения зоны, слоновья и т.д.
- **Обнаружение событий**: Обнаружение людей, транспортных средств и объектов с оценкой достоверности
- **Система оповещений**: Уведомления по нескольким каналам (Telegram, Email, Webhook)
- **Обработка видео**: Обработка на основе CPU с использованием OpenCV и YOLOv8

### Технические характеристики
- **Django 5.x Backend** с REST API
- **React 18 Frontend** с Ant Design Pro
- **PostgreSQL** с PostGIS для геопространственных данных
- **Redis** для кэширования и очередей
- **Apache Kafka** для потоковой передачи событий
- **MinIO** для хранения видео (совместимый с S3)
- **Prometheus + Grafana** для мониторинга
- **Docker Compose** для оркестрации

## 🏗️ Архитектура

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Frontend      │    │    Backend       │    │  Analytics      │
│   (React)       │◄──►│   (Django)       │◄──►│  (Python/YOLO)  │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                               │
                     ┌─────────┴─────────┐
                     │   Message Queue   │
                     │    (Kafka)        │
                     └─────────┬─────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
┌────────▼───────┐    ┌────────▼───────┐    ┌────────▼─────┐
│  Database      │    │  Cache/Queue   │    │  Storage     │
│ (PostgreSQL)   │    │    (Redis)     │    │   (MinIO)    │
└────────────────┘    └────────────────┘    └──────────────┘
```

## 📋 Системные требования

### Аппаратное обеспечение (один сервер)
- **CPU**: Intel Xeon или Core i7/i9 (рекомендуется 8+ ядер)
- **RAM**: 32 ГБ+ (рекомендуется 64 ГБ для 100 камер)
- **Хранилище**: 2 ТБ+ SSD для хранения видео
- **Сеть**: Гигабитный Ethernet

### Программное обеспечение
- **ОС**: Linux (Ubuntu 20.04+ или CentOS 8+)
- **Docker**: 20.10+
- **Docker Compose**: 2.0+

## 🚀 Быстрый старт

### Предварительные условия
```bash
# Установка Docker и Docker Compose
sudo apt update
sudo apt install docker.io docker-compose-plugin
sudo usermod -aG docker $USER
```

### Установка
```bash
# Клонирование репозитория
git clone https://github.com/your-org/insightcore.git
cd insightcore

# Копирование файла окружения и настройка
cp .env.example .env
# Редактирование файла .env с вашей конфигурацией

# Запуск сервисов
docker-compose up -d

# Инициализация базы данных
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py createsuperuser

# Доступ к приложению
# Frontend: http://localhost:3001
# Admin: http://localhost:8000/admin
# Grafana: http://localhost:3000
```

## 🏗️ Структура проекта

```
insight-core/
├── backend/                 # Django backend
│   ├── core/               # Настройки, URL-адреса, промежуточное ПО
│   ├── cameras/            # Модели камер и API
│   ├── analytics/          # Модели аналитики и правила
│   ├── videos/             # Модели обработки видео
│   ├── events/             # Модели событий
│   ├── alerts/             # Система оповещений
│   └── api/                # Конечные точки REST API
├── frontend/               # React frontend
│   ├── src/
│   │   ├── components/     # Повторно используемые компоненты
│   │   ├── pages/          # Компоненты страниц
│   │   ├── api/            # Вызовы API
│   │   └── App.jsx         # Главное приложение
├── analyzer/               # Сервис анализа видео
├── monitoring/             # Конфигурации Prometheus/Grafana
└── docker-compose.yml      # Оркестрация Docker
```

## 🔧 Конфигурация

### Переменные окружения
Создайте файл `.env` со следующими переменными:

```bash
# Настройки Django
DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# База данных
POSTGRES_DB=insightcore
POSTGRES_USER=insightcore_user
POSTGRES_PASSWORD=insightcore_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=insightcore-videos

# Аналитика
ANALYZER_MODEL_PATH=/models/yolov8n.pt
ANALYZER_CONFIDENCE_THRESHOLD=0.5

# Frontend
FRONTEND_API_URL=http://localhost:8000
FRONTEND_WS_URL=ws://localhost:8000
```

## 📊 Возможности аналитики

### Обнаружение объектов
- Обнаружение людей
- Обнаружение транспортных средств (автомобили, грузовики, мотоциклы и т.д.)
- Обнаружение сумок/пакетов
- Пользовательские классы объектов

### Аналитика поведения
- Обнаружение пересечения линии
- Обнаружение нарушения зоны
- Обнаружение слоновья
- Объект, оставленный без присмотра
- Обнаружение скорости
- Аналитика подсчета

### Движок правил
- **Пересечение линии**: Обнаружение объектов, пересекающих виртуальные линии
- **Нарушение зоны**: Мониторинг запретных зон
- **Слоновье**: Обнаружение продолжительного присутствия
- **Обнаружение скорости**: Мониторинг скорости транспортных средств
- **Подсчет**: Подсчет объектов, проходящих через зоны

## 🔔 Система оповещений

### Каналы
- **Telegram**: Уведомления бота
- **Email**: Уведомления по SMTP
- **Webhook**: HTTP-обратные вызовы
- **SMS**: (Скоро)

### Типы оповещений
- **События безопасности**: Неавторизованный доступ, нарушения запретных зон
- **События безопасности**: Нарушения скорости, слоновье
- **Операционные события**: Обнаружение оборудования, отслеживание инвентаря

## 📈 Мониторинг и аналитика

### Особенности панели мониторинга
- Потоки камер в реальном времени
- Хронология событий
- Диаграммы аналитики
- Управление оповещениями
- Мониторинг состояния системы

### Собираемые метрики
- Состояние и работоспособность камеры
- Скорость обнаружения событий
- Производительность обработки
- Использование системных ресурсов
- Время отклика оповещений

## 🛠️ Разработка

### Разработка Backend
```bash
# Установка зависимостей Python
cd backend
pip install -r requirements.txt

# Запуск сервера разработки
python manage.py runserver
```

### Разработка Frontend
```bash
# Установка зависимостей Node.js
cd frontend
npm install

# Запуск сервера разработки
npm start
```

### Запуск тестов
```bash
# Тесты Backend
cd backend
python -m pytest

# Тесты Frontend
cd frontend
npm test
```

## 🚢 Развёртывание

### Развёртывание в продакшн
```bash
# Сборка и развертывание с помощью Docker Compose
docker-compose -f docker-compose.prod.yml up -d

# Или использовать манифесты Kubernetes (скоро)
kubectl apply -f k8s/
```

### Мониторинг
- **Prometheus**: Сбор метрик
- **Grafana**: Панель управления и визуализация
- **AlertManager**: Маршрутизация оповещений (скоро)

## 🔐 Безопасность

### Аутентификация
- Аутентификация на основе JWT
- Контроль доступа на основе ролей
- Управление токенами API

### Защита данных
- Шифрованное хранение видео
- Защищенные конечные точки API
- Регулярные обновления безопасности

## 📚 Документация по API

API построен с использованием Django REST Framework и предоставляет исчерпывающие конечные точки для всей функциональности:

### Аутентификация
- `/api/auth/token/` - Получение токена JWT
- `/api/auth/token/refresh/` - Обновление токена
- `/api/auth/token/verify/` - Проверка токена

### Камеры
- `/api/cameras/` - Управление камерами
- `/api/zones/` - Конфигурация зон
- `/api/lines/` - Конфигурация линий

### Аналитика
- `/api/events/` - Управление событиями
- `/api/rules/` - Конфигурация правил
- `/api/alerts/` - Управление оповещениями

## 🤝 Сотрудничество

Мы приветствуем вклад! Пожалуйста, ознакомьтесь с нашим [Руководством по сотрудничеству](CONTRIBUTING.md) для получения подробной информации.

### Руководящие принципы разработки
- Следуйте PEP 8 для кода Python
- Используйте TypeScript для обеспечения безопасности типов во внешнем интерфейсе
- Пишите тесты для всех новых функций
- При необходимости обновляйте документацию

## 📄 Лицензия

Этот проект лицензирован в соответствии с лицензией MIT - подробности см. в файле [LICENSE](LICENSE).

## 🆘 Поддержка

- **Документация**: [Wiki](https://github.com/your-org/insightcore/wiki)
- **Проблемы**: [GitHub Issues](https://github.com/your-org/insightcore/issues)
- **Обсуждения**: [GitHub Discussions](https://github.com/your-org/insightcore/discussions)

---

**InsightCore** - Открытая аналитика видео для нефтяной и газовой промышленности

*Сделано с ❤️ для более безопасных промышленных объектов*